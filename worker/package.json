{
  "name": "cost-accounting-question-bank-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute cost-question-bank --local --file=schema.sql",
    "db:remote": "wrangler d1 execute cost-question-bank --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
